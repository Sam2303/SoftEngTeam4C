/**
 * Check if a string is a valid SHA256 checksum.
 * @param {string} checksum - The string to validate.
 * @returns {boolean} - true if it is valid, otherwise false.
 */
function isSHA256(checksum) {
    // TODO: this
    return true;
}

module.exports = {
    isSHA256,
};
